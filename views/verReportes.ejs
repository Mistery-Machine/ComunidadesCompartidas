<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>Gestión de Reportes</title>
    <link rel="stylesheet" href="/css/verReportes.css"/>
    <style>
        .estado-select {
            padding: 5px 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: white;
            font-size: 14px;
            margin-top: 10px;
        }
        .estado-pendiente { color: #f39c12; font-weight: bold; }
        .estado-proceso { color: #3498db; font-weight: bold; }
        .estado-resuelto { color: #27ae60; font-weight: bold; }
        .info-reporte {
            margin: 10px 0;
            font-size: 14px;
            color: #666;
        }
    </style>
</head>
<%- include("templates/navbar") %>
<body>
<main class="contenedor-eventos">
    <h1 style="text-align: center; margin-bottom: 30px; color: #333;">Gestión de Reportes</h1>
    
    <% if (reportes && reportes.length > 0) { %>
        <div class="grid-eventos">
            <% reportes.forEach(reporte => { %>
                <div class="tarjeta-evento">
                    <h2 class="titulo-tarjeta"><%= reporte.tipo || 'Reporte' %></h2>
                    <p class="descripcion-evento"><%= reporte.descripcion %></p>
                    
                    <div class="info-reporte">
                        <p><strong>Autor:</strong> <%= reporte.Autor %></p>
                        <p><strong>Fecha:</strong> <%= new Date(reporte.fecha).toLocaleDateString('es-ES') %></p>
                    </div>

                    <p class="estado-evento estado-<%= reporte.estado.toLowerCase().replace(' ', '-') %>">
                        Estado: <%= reporte.estado %>
                    </p>
                    
                    <select class="estado-select" onchange="cambiarEstadoReporte('<%= reporte._id %>', this.value)">
                        <option value="Pendiente" <%= reporte.estado === 'Pendiente' ? 'selected' : '' %>>Pendiente</option>
                        <option value="En proceso" <%= reporte.estado === 'En proceso' ? 'selected' : '' %>>En proceso</option>
                        <option value="Resuelto" <%= reporte.estado === 'Resuelto' ? 'selected' : '' %>>Resuelto</option>
                    </select>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <div style="text-align: center; padding: 50px; color: #666;">
            <h3>No hay reportes disponibles</h3>
            <p>Aún no se han registrado reportes en el sistema.</p>
        </div>
    <% } %>
</main>
<%- include("templates/footer") %>

<script>
    // Cambiar estado de reporte
    async function cambiarEstadoReporte(id, nuevoEstado) {
        try {
            const response = await fetch(`/dashboard/reporte/${id}/estado`, {
                method: "PATCH",
                headers: {
                    "Content-Type": "application/json",
                },
                body: JSON.stringify({ estado: nuevoEstado }),
            });

            if (response.ok) {
                // Recargar la página para mostrar los cambios
                window.location.reload();
            } else {
                alert("Error al cambiar el estado del reporte");
            }
        } catch (error) {
            console.error("Error:", error);
            alert("Error al cambiar el estado del reporte");
        }
    }
</script>
</body>
</html>
